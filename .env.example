# URL de l'API Laravel (sans slash final)
# En développement avec le proxy Vite : laisser vide (proxy /api → localhost:8000)
# En production : mettre l'URL complète du backend
VITE_API_URL=http://localhost:8000/api
